{
    "name": "mutual-fund-rag-chatbot-root",
    "version": "1.0.0",
    "private": true,
    "engines": {
        "node": ">=18.0.0"
    },
    "scripts": {
        "build": "cd Phase4_Frontend/frontend && npm install && npm run build",
        "postbuild": "cp -r Phase4_Frontend/frontend/dist public",
        "install": "cd Phase4_Frontend/frontend && npm install"
    }
}